//import _ from 'lodash';
// const fs = require('fs');
// const path = require('path');
// const { ipcRenderer } = window.require('electron');


const FILEMOVE = (a, b) => {
    console.log(a)
    // const selectedFiles = selectedRow.map(({ key, location }) => {
    //     return [key,location]
    // })
}


module.exports = { FILEMOVE };